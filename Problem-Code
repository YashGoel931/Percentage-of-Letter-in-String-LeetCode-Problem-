class Solution {
    public int percentageLetter(String s, char letter) {
     int count=0;
     for(int i=0 ; i<s.length() ; i++){
      int z=Character.compare(s.charAt(i),letter);
      if(z==0)
         count++;
     }
    return (count*100)/s.length();
    }
}
